// É uma representação de um espaço na memória do computador para transitar dados rapidamente
// Guarda de maneira binária - hexadecimal

const buf = Buffer.from('ok')

console.log(buf.toJSON())
